<!DOCTYPE html>
<head>
        <title>
            Task Example
        </title>
        <link rel="stylesheet" href="/static/stylesheets/style.css" type="text/css" />
        <script>
            function done(todoTag){
                const _id = todoTag.getAttribute("data-todoId");
                fetch('/done', {
                    method: 'PUT',
                    body: JSON.stringify({ id: `${_id}`}),
                    headers: {
                        "Content-Type": "application/json",
                    }
                })
                .catch((error) => {
                    console.error('Error:', error);
                });
    
            }
            function deleteItem(todoTag){
                const _id = todoTag.getAttribute("data-todoId");
                fetch(`/delete/${_id}`, {    // localhost/t89u0dh9eh7er <- ID
                    method: 'DELETE',
                    headers: {
                        "Content-Type": "application/json",
                    }
                })
                .catch((error) => {
                    console.error('Error:', error);
                });
            }
        
            //write the API call
            </script>
    </head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 text text-center">
                <p id="comicTitle">"{{ comic.title}}"</p> 
                <p id="comicName">- XKCD </p>
    
                <!-- random button -->
                <form class="form-button" action="/" method="post">
                    <a href="/id/{{ random 1 614 }}"><p class="form-control">Random</p></a>
                </form>
    
                <!-- Comic and Date -->
                <img src={{ comic.img }} alt={{ comic.alt }} class="ComicData">                    
                <p id="comicDate">{{ comic.month }}/{{ comic.day }}/{{ comic.year }}</p>
    
    
            </div>
        </div>
    </div>  
</body>